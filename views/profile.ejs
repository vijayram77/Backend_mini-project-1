<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        html,
        body {
            width: 100%;
            height: 100%;
        }
    </style>
</head>

<body>
    <div class="bg-zinc-900 w-full min-h-screen px-[5vw] py-[5vh]">
        
        <div class="flex justify-between">
            <a class="text-blue-500 font-semibold text-xl py-2 rounded-md" href="/feed">Goo To Feed</a>
            <a class="text-red-400 font-semibold py-2 px-4 rounded-md bg-red-900/20" href="/logout">Logout</a>
        </div>
        <h1 class="text-2xl font-semibold text-zinc-200 tracking-tighter pb-2">
            Profile
        </h1>
        <h1 class="text-3xl font-[400] text-zinc-200 tracking-tighter pb-2"><span class="font-light">Hello</span>
            <%= user.name %> 👋
        </h1>
        <h6 class="text-xl font-semibold text-zinc-200 tracking-tighter pb-2"><span class="font-light">create a new post
        </h6>

        <form method="post" action="/createpost">
            <textarea required
                class="px-6 py-2 outline-none text-white w-full resize-none py-2 mb-2 bg-transparent block rounded-md border border-zinc-600"
                placeholder="whats on your mind?" name="content"></textarea>
            <input name="ImageUrl" required class="px-6 outline-none text-white border border-zinc-600 w-full py-2 bg-transparent block cursor-pointer text-white rounded-md" type="text"
                    placeholder="Add Image Url" />
            <input class="px-6 py-2 bg-blue-500 block mt-2 cursor-pointer text-white rounded-md" type="submit"
                value="Create New Post" />
        </form>
        <div>
            <h6 class="text-2xl mt-20 font-semibold text-zinc-200 tracking-tighter capitalize pb-2">your posts</h6>
            <div class="flex flex-col gap-4">
                <% if(posts.length === 0) { %>
                    <h6 class="text-[16px] mt-4 font-semibold text-zinc-500 tracking-tighter capitalize pb-2">No posts Available</h6>
                <% } %>
                <% posts.reverse().forEach((val) => { %>
                    <div class="px-4 py-3 bg-zinc-800 border-[1px] border-zinc-700 w-full rounded-md ">
                        <a class="text-blue-500 mb-3" href="">@<%= user.username %> </a>
                        <p class="text-zinc-500 mb-3">
                            <%= val.content %>
                        </p>
                        <div class=" h-[160px] my-2">
                            <img alt="img" class="object-cover h-full" src="<%= val.ImageUrl %>" alt="">
                        </div>
                        <div class="flex gap-4 items-center">
                            <a class="text-blue-500" href="/like/<%= val._id %>"><%= val.likes.length %> Likes</a>
                            <a class="text-zinc-500" href="/edit/<%= val._id %>">Edit</a>
                        </div>
                    </div>
                <% }) %>
                
                
            </div>
        </div>
    </div>
</body>

</html>