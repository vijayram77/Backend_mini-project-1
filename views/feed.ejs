<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        html,
        body {
            width: 100%;
            height: 100%;
        }
    </style>
</head>

<body>
    <div class="bg-zinc-900 w-full min-h-screen px-[5vw] py-[5vh]">
        
        <div class="flex justify-between">
            <a class="text-blue-500 font-semibold text-xl py-2 rounded-md" href="/profile">Go To Profile</a>
        </div>
        <h1 class="text-2xl font-semibold text-zinc-200 tracking-tighter pb-2">
            Feed
        </h1>
    
        <% posts.reverse().forEach((val) => { %>
            <div class="px-4 py-3 bg-zinc-800 border-[1px] border-zinc-700  my-2 w-full rounded-md ">
                <a class="text-blue-500 mb-3" href="">@<%= val.username %> </a>
                <p class="text-zinc-500 mb-3">
                    <%= val.content %>
                </p>
                <div class=" h-[160px] my-2">
                    <img alt="img" class="object-cover h-full" src="<%= val.ImageUrl %>" alt="">
                </div>
                <div class="flex gap-4 items-center">
                    <a class="text-blue-500" href="/feed/like/<%= val._id %>"><%= val.likes.length %> Likes</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>

</html>