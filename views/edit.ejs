<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        html,
        body {
            width: 100%;
            height: 100%;
        }
    </style>
</head>

<body>
    <div class="bg-zinc-900 w-full min-h-screen px-[5vw] py-[5vh]">
    <h6 class="text-xl font-semibold text-zinc-200 tracking-tighter capitalize pb-2"><span class="font-light">Update
            Your Post
    </h6>

    <form method="post" action="/update/<%= post._id %>">
        <textarea
            class="px-6 py-2 outline-none text-zinc-300 w-full resize-none py-2 mb-2 bg-transparent block rounded-md border border-zinc-600"
            placeholder="Change Your Post Content" name="content"><%= post.content %></textarea>
        <input class="px-6 py-2 bg-yellow-600 block cursor-pointer text-white rounded-md" type="submit"
            value="Update Post" />
    </form>
    </div>
</body>

</html>